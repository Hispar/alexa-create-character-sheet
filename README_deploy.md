Despliegue en lambda
====================

Modificar en `lambda/py/lambda_function.py`

#### Manual

Ejecutar los siguientes comandos:

`cp -r py/* skill_env/`

`cd skill_env/`

`zip -r ../test.zip .`

#### Script

Ejecutar el script:

`./bundle.sh`


Subir `test.zip`

